services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: resonance
      POSTGRES_PASSWORD: resonance
      POSTGRES_DB: resonance
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U resonance"]
      interval: 5s
      timeout: 3s
      retries: 5

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - miniodata:/data
    ports:
      - "9002:9000"
      - "9001:9001"

  backend:
    platform: linux/amd64
    build:
      context: ./backend
      platforms:
        - linux/amd64
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DATABASE_URL: postgresql+asyncpg://resonance:resonance@db:5432/resonance
      S3_ENDPOINT: http://minio:9000
      S3_PUBLIC_ENDPOINT: http://127.0.0.1:9002
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      S3_BUCKET_TRACKS: resonance-tracks
      CLAP_DEVICE: cpu
      OMP_NUM_THREADS: "6"
      MKL_NUM_THREADS: "6"
      VECLIB_MAXIMUM_THREADS: "6"
      TORCH_NUM_THREADS: "6"
      CORS_ORIGINS: '["http://localhost:3000","http://127.0.0.1:3000"]'
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - models:/models

  worker:
    platform: linux/amd64
    image: project-resonance-backend
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      DATABASE_URL: postgresql+asyncpg://resonance:resonance@db:5432/resonance
      S3_ENDPOINT: http://minio:9000
      S3_PUBLIC_ENDPOINT: http://127.0.0.1:9002
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      S3_BUCKET_TRACKS: resonance-tracks
      CLAP_DEVICE: cpu
      OMP_NUM_THREADS: "6"
      MKL_NUM_THREADS: "6"
      VECLIB_MAXIMUM_THREADS: "6"
      TORCH_NUM_THREADS: "6"
    volumes:
      - ./backend:/app
      - models:/models
    command: python -u -c "import app.core.patches; from app.workers.analyze import run_worker; run_worker()"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000/api
    ports:
      - "3000:3000"

volumes:
  pgdata:
  miniodata:
  models: